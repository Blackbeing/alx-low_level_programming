#include <stdio.h>

/**
 * main -- Print single digit numbers
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int a = 0;

	for (a = 0; a <= 9; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
